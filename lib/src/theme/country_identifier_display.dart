/// Controls whether the country selector shows a flag emoji or an ISO code.
enum CountryIdentifierDisplay {
  /// Show the country flag emoji (e.g. "🇪🇬").
  flag,

  /// Show the ISO 3166-1 alpha-2 code (e.g. "EG").
  isoCode,
}
